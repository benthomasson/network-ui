/* Copyright (c) 2017 Red Hat, Inc. */

const templateUrl = require('~network-ui/application/stream.partial.svg');

function stream () {
  return { restrict: 'A', templateUrl};
}
exports.stream = stream;
